/**
 * Static list of manifest URLs served by this container.
 * In a future version this would be fetched from a discovery API.
 */
export const MANIFEST_URLS: string[] = ["/manifests/hello-app.json"];
