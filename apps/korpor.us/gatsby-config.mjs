const config = {
  siteMetadata: {
    title: "Korporus",
    description: "Federated application platform with a shell host, runtime module loading, and Web Component contracts.",
    siteUrl: "https://korpor.us"
  },
  graphqlTypegen: false,
  plugins: ["gatsby-plugin-postcss"]
};

export default config;
